package cdr.modified;

import cdr.geometry.primitives.adapter.GeometryAdapter;
import cdr.graph.datastructure.GraphVertex;

public abstract class GraphVertexAdapter<T> implements GeometryAdapter<GraphVertex, T> {

}
